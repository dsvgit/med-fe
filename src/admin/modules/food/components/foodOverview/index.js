import React, {
  PropTypes,
} from 'react';

const FoodOverview = (props) => {
  return (
    <div>Food list</div>
  );
};

FoodOverview.propTypes = {};
FoodOverview.defaultProps = {};

export default FoodOverview;
